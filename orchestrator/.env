# PostgreSQL credentials (for the 'db' service and Django settings)
POSTGRES_USER=orch_user
POSTGRES_PASSWORD=orch_pass
POSTGRES_DB=orch_db

# VT API (where to callback with mask and status)
VT_API_URL=https://vt.example.com/api/v1
VT_API_TOKEN=your_shared_secret_token

# Django SECRET_KEY and DEBUG (optional)
DJANGO_SECRET_KEY=replace_this_with_a_random_string
DJANGO_DEBUG=0
